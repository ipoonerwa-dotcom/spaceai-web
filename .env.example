# SpaceAIBank contract address — leave EMPTY until the bank is deployed.
# While empty the console shows "星际银行即将开放" instead of calling a dead address.
# After deploying, set this and redeploy the site.
VITE_BANK_ADDRESS=
